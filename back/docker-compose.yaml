services:
  mqtt:
    image: eclipse-mosquitto:latest
    container_name: ecogarden-broker
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/config:/mosquitto/config
    restart: always

  # api:
  #   build: .
  #   depends_on:
  #     - mqtt
  #   environment:
  #     - MQTT_HOST=mqtt 
  #     - MQTT_PORT=1883